import React from "react";
import StudyDestinations from "../Pages/Destination/StudyDestination";

const SlidingUniversities = () => {
	return (
		<></>
		// <div className="relative overflow-hidden w-full h-32 bg-gray-100 mt-6">
		// 	<div className="absolute p-6 inset-0 overflow-hidden">
		// 		<div className="flex space-x-12 animate-marquee">
		// 			{StudyDestinations.map((university) => (
		// 				<div key={university.id} className="flex flex-col items-center justify-center">
		// 					<img src={`${university.region_img}`} alt={`${university.University} Logo`} className="w-20 h-20 object-cover rounded-full" />
		// 				</div>
		// 			))}
		// 		</div>
		// 	</div>
		// </div>
	);
};

export default SlidingUniversities;
